# 简历项目描述

## 桌面陪伴机器人控制与通信仿真系统 | Python + Tkinter

- 使用 Python 和 Tkinter 开发不依赖实体硬件的桌面机器人仿真软件，将 GUI、
  虚拟传感器、行为状态机、优先级任务队列、运动仿真及日志模块进行分层设计。
- 设计启动、待机、任务执行、避障、低电量、休眠和故障等 8 种状态，统一处理
  用户命令与传感器事件，并对非法状态跳转进行校验。
- 基于优先级队列实现 6 类机器人任务的调度，支持紧急任务抢占、任务恢复和
  执行过程状态展示。
- 模拟距离、电量、温度及通信状态，实现障碍物检测、低电量保护、连续通信超时、
  传感器失效和过温保护等异常处理。
- 设计 `AA55 + 版本 + 命令 + 序号 + 长度 + Payload + CRC16` 二进制协议，
  实现流式拆包/粘包、噪声重同步、错误统计、512 字节缓冲上限及 100 ms 半帧超时。
- 实现协议网关，将心跳、传感器和远程控制帧映射到控制器，增加Payload范围校验、
  重复序号过滤、任务队列背压与拒绝统计及设备状态回传，防止错误数据、重复命令或
  持续指令堆积直接影响系统状态。
- 封装通信服务统一管理增量接收、协议分发、链路超时和断线恢复，模拟嵌入式设备
  与上位机联调中的完整数据通路。
- 基于TCP搭建无硬件虚拟设备和联调客户端，将自定义协议运行在真实Socket字节流上，
  支持多次连接、断开检测和设备状态应答；使用互斥锁保护共享控制器，并限制并发连接数。
- 编写 60 项自动化测试及 10000 帧随机分块压力测试，覆盖任务调度、拆包/粘包、
  CRC/长度错误恢复、绝对超时和网关输入校验；通过GitHub Actions跨平台验证。

## 面试介绍

这个项目的重点不是机器人动画，而是控制软件架构。我把控制器和界面分离，
通过周期 tick 模拟嵌入式任务运行；用状态机组织机器人行为，用优先级队列调度
任务，并通过虚拟传感器构造正常和异常输入。我还增加了面向 UART DMA 数据流的
二进制通信协议，通过增量解析器处理拆包、粘包、噪声和错误帧。项目提供故障注入、
恢复和自动化测试，使状态切换、异常路径和通信边界都能够复现。
